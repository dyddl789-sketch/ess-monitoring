<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lgy.ess_monitoring.dao.EssDeviceDAO">

    <!-- 기기 등록 -->
    <insert id="insertDevice" parameterType="com.lgy.ess_monitoring.dto.EssDeviceDTO">
        insert into ess_device
        (
            member_id,
            group_id,
            device_name,
            location,
            capacity_kw,
            device_type,
            status,
            install_date,
            latitude,
            longitude,
            nx,
            ny,
            is_main
        )
        values
        (
            #{member_id},
            #{group_id},
            #{device_name},
            #{location},
            #{capacity_kw},
            #{device_type},
            #{status},
            #{install_date},
            #{latitude},
            #{longitude},
            #{nx},
            #{ny},
            'N'
        )
    </insert>

    <!-- 기기 목록 -->
    <select id="getDeviceList" resultType="com.lgy.ess_monitoring.dto.EssDeviceDTO">
        select *
        from ess_device
        where member_id = #{param1}
        order by device_id desc
    </select>

    <!-- 기기 수 -->
    <select id="getDeviceCount" resultType="int">
        select count(*)
        from ess_device
        where member_id = #{param1}
    </select>

    <!-- 삭제 -->
    <delete id="deleteDevice">
        delete from ess_device
        where device_id = #{device_id}
          and member_id = #{member_id}
    </delete>

    <!-- 상세 -->
	<select id="deviceDetail" parameterType="int" resultType="com.lgy.ess_monitoring.dto.EssDeviceDTO">
	    SELECT
	        device_id,
	        member_id,
	        device_name,
	        location,
	        capacity_kw,
	        device_type,
	        status,
	        install_date,
	        latitude,
	        longitude,
	        nx,
	        ny
	    FROM ess_device
	    WHERE device_id = #{device_id}
	</select>

</mapper>
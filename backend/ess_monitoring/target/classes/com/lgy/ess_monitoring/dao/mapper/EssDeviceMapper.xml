<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lgy.ess_monitoring.dao.EssDeviceDAO">

    <!-- ESS 기기 등록 -->
    <insert id="insertDevice" parameterType="com.lgy.ess_monitoring.dto.EssDeviceDTO">
        insert into ess_device
        (
            member_id,
            group_id,
            device_name,
            location,
            capacity_kw,
            device_type,
            status,
            install_date
        )
        values
        (
            #{member_id},
            #{group_id},
            #{device_name},
            #{location},
            #{capacity_kw},
            #{device_type},
            #{status},
            #{install_date}
        )
    </insert>

    <!-- 로그인한 회원의 ESS 기기 목록 조회 -->
    <select id="getDeviceList" resultType="com.lgy.ess_monitoring.dto.EssDeviceDTO">
        select
            device_id,
            member_id,
            group_id,
            device_name,
            location,
            capacity_kw,
            device_type,
            status,
            install_date
        from ess_device
        where member_id = #{param1}
        order by device_id desc
    </select>

    <!-- 로그인한 회원의 ESS 기기 수 조회 -->
    <select id="getDeviceCount" resultType="int">
        select count(*)
        from ess_device
        where member_id = #{param1}
    </select>
    
	<!-- 기기 삭제 -->
	<delete id="deleteDevice">
		delete from ess_device
		 where device_id = #{device_id}
		   and member_id = #{member_id}
	</delete>

</mapper>





